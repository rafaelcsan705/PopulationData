//
//  ContentView.swift
//  PopulationDataApp
//
//  Created by Rafael Santos on 20/09/2024.
//

import SwiftUI

struct ContentView: View {
    var body: some View {
        VStack {
            Image(systemName: "globe")
                .imageScale(.large)
                .foregroundStyle(.tint)
            Text("Hello, world!")
            Text("Let's start the challenge.")
        }
        .padding()
    }
}

#Preview {
    ContentView()
}
